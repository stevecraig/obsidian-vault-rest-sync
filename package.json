{
	"name": "obsidian-remote-vault-sync",
	"version": "2.0.0",
	"description": "Obsidian plugin to sync files from a remote REST API",
	"main": "main.js",
	"scripts": {
		"build": "node esbuild.config.mjs production",
		"dev": "node esbuild.config.mjs",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"keywords": [
		"obsidian",
		"sync",
		"vault"
	],
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^22.0.0",
		"esbuild": "^0.24.0",
		"obsidian": "latest",
		"typescript": "^5.9.3",
		"vitest": "^4.0.18"
	}
}
